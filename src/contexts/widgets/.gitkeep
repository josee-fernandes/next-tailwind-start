Contextos que serão iniciados quando o usuário estiver acessando partes específicas do site.
Exemplo: public + private + CheckoutContext + CartContext.
Quanto menos melhor.

// // // // // // // // // // // // // // // // //

Contexts that will be launched when the user is accessing specific parts of the site.
Example: public + private + CheckoutContext, CartContext.
The less the better.